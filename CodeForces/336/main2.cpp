#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <stdio.h>
#include <stdlib.h>

using namespace std;

int main()
{
	int lena=0, lenb[10000], len=0;

	string a, b;
	cin >> a;

	for(i=0;i<a.length();i++)
		lena += a[i]-'0';

	cin >> b;
	for(i=0;i<b.length();i++)
	{

	}
}